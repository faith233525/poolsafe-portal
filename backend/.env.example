
#############################################
# Backend Environment Variables (Example)   #
#############################################

# Server
PORT=4000
NODE_ENV=development
LOG_LEVEL=info

# Database
# For dev SQLite (Prisma): file:./dev.db
DATABASE_URL="file:./dev.db"
# For production, use PostgreSQL (default schema 'public'):
# DATABASE_URL=postgresql://user:password@localhost:5432/loungenie?schema=public
# Optional: set PRISMA_SCHEMA to the Postgres schema file for CLI operations (migrate/generate)
# PRISMA_SCHEMA=backend/prisma/schema.postgres.prisma

# JWT / Auth
JWT_SECRET="replace-with-long-random-secret"

# Uploads
UPLOAD_MAX_SIZE_MB=2

# Rate Limiting
RATE_LIMIT_GLOBAL_MAX=100
RATE_LIMIT_GLOBAL_WINDOW_MS=60000
RATE_LIMIT_LOGIN_MAX=10
RATE_LIMIT_LOGIN_WINDOW_MS=60000
RATE_LIMIT_REGISTER_MAX=5
RATE_LIMIT_REGISTER_WINDOW_MS=60000
RATE_LIMIT_UPLOAD_MAX=10
RATE_LIMIT_UPLOAD_WINDOW_MS=60000
RATE_LIMIT_NOTIFICATIONS_MAX=20
RATE_LIMIT_NOTIFICATIONS_WINDOW_MS=60000

# Azure AD SSO Integration
# AZURE_CLIENT_ID=your-azure-client-id
# AZURE_TENANT_ID=your-azure-tenant-id
# AZURE_CLIENT_SECRET=your-azure-client-secret
# AZURE_REDIRECT_URI=http://localhost:4000/api/auth/callback

# HubSpot CRM Integration (Account ID: 21854204)
# HUBSPOT_API_KEY=your-hubspot-api-key

# Email/SMTP Configuration
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_USER=your-email@domain.com
# SMTP_PASS=your-email-password
# SMTP_FROM=noreply@poolsafeinc.com

# Public URLs
# FRONTEND_URL=https://portal.poolsafeinc.com

# Support/Admin emails used in notifications and error pages
# SUPPORT_EMAIL=support@poolsafeinc.com
# SUPPORT_ADMIN_EMAIL=engineering@poolsafeinc.com

# Seed overrides for creating initial admin/support users (optional)
# SUPPORT_EMAIL_SEED=support@poolsafeinc.com
# SUPPORT_PASSWORD_SEED=LounGenie123!!
# ADMIN_EMAIL_SEED=admin@poolsafeinc.com
# ADMIN_PASSWORD_SEED=ChangeMe123!!

# (Future) File storage (local or S3 style)
# FILE_STORAGE_DIR=./uploads

#############################################
# Copy to .env and adjust values for local.  #
# Never commit the real .env file.           #
#############################################
