# Frontend environment variables (Vite)
# Base URL for API calls from the browser. Leave empty to use same-origin (recommended when frontend and backend are served from the same domain).
VITE_API_BASE_URL=
